<p><%= comment.user.name %>: <%= comment.body %> - <%= time_ago_in_words(comment.created_at) %> ago <%= link_to "delete", review_comment_path(comment.review, comment), method: :delete, data: {confirm: 'Are you sure?'} if current_user == comment.review.user || current_user == comment.user %></p>